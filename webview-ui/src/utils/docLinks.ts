/**
 * Utility for building extension help links.
 *
 * リポジトリ移設に伴い doc リンクの基点は無効化している（外部リンク先を持たない）。
 * 移設先が決まったら、ここで基点 URL を返すよう戻せば全 "Learn more" リンクが復活する。
 */
export function buildDocLink(_path: string, _campaign: string): string {
	return ""
}
